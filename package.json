{
  "name": "gm_extension",
  "version": "1.0.0",
  "description": "",
  "main": "extension.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "upload" : "uglifyjs extension.js -o extension.min.js && git add . && git commit -m 'minify' && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uglify-js": "^3.17.4"
  }
}
